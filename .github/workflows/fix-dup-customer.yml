name: Fix duplicate customer (temporary)

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

env:
  AWS_REGION: ${{ secrets.AWS_REGION }}
  AWS_ROLE_ARN: ${{ secrets.AWS_ROLE_ARN }}
  EC2_INSTANCE_ID: ${{ secrets.EC2_INSTANCE_ID }}

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ env.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Backup then delete empty duplicate customer row
        id: ssm
        run: |
          cat > /tmp/query.json << 'EOF'
          {"commands": [
            "set -e",
            "cp --reflink=auto /opt/evn-warp-runtime/database/dev.db /opt/evn-warp-runtime/backups/dev-$(date +%Y%m%d-%H%M%S)-pre-dupcustomer-fix.db",
            "ls -la /opt/evn-warp-runtime/backups | tail -5",
            "cd /opt/evn-warp",
            "node -e \"const {createClient}=require('@libsql/client');const c=createClient({url:'file:/opt/evn-warp-runtime/database/dev.db'});const id='cmsybpitl001101sa5ucqmyu9';(async()=>{const before=await c.execute('SELECT COUNT(*) as n FROM \\\"Customer\\\" WHERE id = ?', [id]);console.log('before:', JSON.stringify(before.rows));await c.execute('DELETE FROM \\\"Customer\\\" WHERE id = ?', [id]);const after=await c.execute('SELECT COUNT(*) as n FROM \\\"Customer\\\" WHERE id = ?', [id]);console.log('after:', JSON.stringify(after.rows));})().catch(e=>{console.error(e);process.exit(1)});\""
          ]}
          EOF

          command_id="$(aws ssm send-command \
            --instance-ids "$EC2_INSTANCE_ID" \
            --document-name AWS-RunShellScript \
            --comment "EVN-WARP fix duplicate customer 김현수" \
            --timeout-seconds 60 \
            --parameters file:///tmp/query.json \
            --query 'Command.CommandId' \
            --output text)"
          echo "command_id=$command_id" >> "$GITHUB_OUTPUT"

      - name: Get result
        env:
          COMMAND_ID: ${{ steps.ssm.outputs.command_id }}
        run: |
          for i in $(seq 1 15); do
            status="$(aws ssm get-command-invocation \
              --command-id "$COMMAND_ID" \
              --instance-id "$EC2_INSTANCE_ID" \
              --query Status --output text 2>/dev/null || echo Pending)"
            echo "[$i] $status"
            case "$status" in
              Success|Failed|Cancelled|TimedOut) break ;;
            esac
            sleep 4
          done
          echo "=== STDOUT ==="
          aws ssm get-command-invocation \
            --command-id "$COMMAND_ID" \
            --instance-id "$EC2_INSTANCE_ID" \
            --query StandardOutputContent --output text || true
          echo "=== STDERR ==="
          aws ssm get-command-invocation \
            --command-id "$COMMAND_ID" \
            --instance-id "$EC2_INSTANCE_ID" \
            --query StandardErrorContent --output text || true
